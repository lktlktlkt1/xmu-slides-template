[project]
name = "paper-bilibili-uploader"
version = "0.1.0"
description = "Validate and upload narrated paper videos to Bilibili"
requires-python = ">=3.10"
dependencies = [
    "biliup>=1.2,<2",
    "psutil>=6",
    "requests>=2.34",
]
